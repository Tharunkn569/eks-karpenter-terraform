apiVersion: v1
kind: Service
metadata:
  name: payments-microservice
  namespace: payments
  labels:
    app: payments-microservice
spec:
  type: ClusterIP
  selector:
    app: payments-microservice
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP