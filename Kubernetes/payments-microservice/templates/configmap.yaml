apiVersion: v1
kind: ConfigMap
metadata:
  name: payments-microservice-config
  namespace: {{ .Values.namespace }}
data:
  APP_NAME: "payments-microservice"
  ENVIRONMENT: "{{ .Values.environment }}"
  LOG_LEVEL: "{{ .Values.config.logLevel }}"